<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Fundo branco para o EditText -->
    <corners android:radius="8dp" /> <!-- Cantos arredondados -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Borda cinza clara -->
</shape>
